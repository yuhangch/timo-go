package api

import (
	"testing"
)

// Test .
func TestTemplate(t *testing.T) {

	// tile := mercantile.NewTile(1, 2, 3)
	// // bbox := tile.XYBounds()
	// // segSize := (bbox.Right - bbox.Left) / 4
	// // fmt.Println(bbox.List())
	// ctx := NewTilesCtx("countries", "geom", "fid", tile)
	// query := sql(ctx)
	// fmt.Println(query)
}
